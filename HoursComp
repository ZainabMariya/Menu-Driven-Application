package group;

/**
 *
 * @author naure
 */
import java.util.Comparator;
public class HoursComp implements Comparator<Student> {
 
 @Override
    public int compare(Student s1 , Student s2 ){
        return s1.getHours() - s2.getHours() ; 
 }
}
